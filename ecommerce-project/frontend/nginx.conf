# server {
#     listen 80;
#     server_name localhost;
#     root /usr/share/nginx/html;
#     index index.html;

#     # 解决React路由刷新404
#     location / {
#         try_files $uri $uri/ /index.html;
#     }

#     # 反向代理后端API（避免跨域）
#     location /api {
#         proxy_pass http://backend:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
# }
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # 新增：Nginx监控接口（供Prometheus抓取数据）
    location /metrics {
        stub_status on;
        access_log off;
        allow all;
    }

    # 前端路由刷新不404（原有功能）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 反向代理后端API（原有功能，对接后端容器）
    location /api/ {
        proxy_pass http://ecommerce-backend:8083/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 健康检查接口（原有功能）
    location /health {
        return 200 "ok";
        add_header Content-Type text/plain;
    }
}